<UserControl x:Class="DataJuggler.WPF.Controls.TabControls.TabButton"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="48" d:DesignWidth="200">
    <Grid x:Name="TabButtonGrid">
        <Grid.Background>
            <ImageBrush ImageSource="../Images\DisabledTab2.png">
            </ImageBrush>
        </Grid.Background>
        <Button Name="LabelButton" Click="LabelButton_Click" Margin="0,-6,0,6">
            <Button.Template>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Label Name="Label" Content="Button" HorizontalAlignment="Stretch" HorizontalContentAlignment="Center"
                        VerticalAlignment="Bottom" FontFamily="Verdana" FontSize="16"></Label>            
                </ControlTemplate>
            </Button.Template>
        </Button>
    </Grid>
</UserControl>
